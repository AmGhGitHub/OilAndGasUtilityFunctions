const logo = require("../../images/GD_Logo3.jpg");

const GravityLogo = () => {
  return (
    <section className="d-flex flex-row justify-content-center mt-3">
      <img src={logo} alt="shap logo" height="150px" />
    </section>
  );
};

export default GravityLogo;
