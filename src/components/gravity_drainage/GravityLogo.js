const logo = require("../../images/GD_Logo.jpg");

const GravityLogo = () => {
  return (
    <section className="d-flex flex-row justify-content-center mt-4">
      <img src={logo} alt="shap logo" height="120px" />
    </section>
  );
};

export default GravityLogo;
